# Context Manifest — BB-241 The Bay hub (SH3)

## Objective

Build The Bay per the SH3 boards (founder-confirmed NOW): the
information architecture (one gold table → who is waiting on me →
today/month → watch → practice → personal), the battle-selection
hierarchy on the PLAY hub, and the SH-Nav route map (THE BAY · PLAY ·
ME hubs; the match tunnel renders no hub navigation). The rematch
letter surfaces the honest empty state (the letter list awaits a
Bay-level endpoint — the rematch offer lives on the result screen per
BB-219b). Future-feature slots are visible labeled placeholders only
(OQ-030 / DEC-031 #6).

## Relevant rule / requirement IDs

- BB-241 inbox contract; SH-BayIA, SH-Battle, SH-Nav, SH-Desk-1,
  SH-Journey; L-012 (captures to current/, replacing); OQ-030 /
  DEC-031 #6; GR-020/UF-05 (AI labelled).

## Files expected to change

- `app/bay/page.tsx` (new), `components/hub/hub-bar.tsx` + `hub.css`
  (new), `components/opening/opening-screen.tsx` (PLAY NOW → /bay),
  `app/play/page.tsx` (hub bar + hierarchy header), e2e entry-helper
  migration across all specs (title → Bay → battle selection),
  `e2e/bay.spec.ts` (new), current/ captures + README.

## Explicitly out of scope

- The features behind the slots (rating P1-M2, rival challenges,
  Today's Deal PDR-10, Monthly Bounty PDR-11, Live Tables); SH4
  result sequence (BB-239); the matchmaking queue.

## Result

**Changed:** the three-hub navigation with the shared bar (desktop
top / mobile bottom tabs), the Bay screen (gold table with the honest
ranked line, letters empty state, three SOON slots, the functional
practice room, the ME card), PLAY NOW → /bay, the Play hub header
(PLAY A PERSON / CHALLENGE SOMEONE hierarchy), spec entry migration
through the Bay (one commit; W1's spec helpers touched — flagged).

**Tests:** typecheck 0 · unit 301 · lint clean · full E2E 22/22
(strict seed) · bay spec 2/2 (placeholders have no controls, no
fabricated data) · captures bay-{desktop,mobile}.png in current/.

**Deviations:** no liquidity/queue numbers fabricated (honest copy);
the letters slot has no list endpoint yet (the rematch offer lives on
the result screen); the ME chip shows UNRATED until P1-M2.
