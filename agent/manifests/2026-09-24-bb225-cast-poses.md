# Context Manifest — BB-225 cast pose-system extension

Filled in per AGENTS.md before any task changes code.

## Objective

Extend the BB-216 live-match character presentation to all 8 cast
characters (DESIGN_ACCEPTANCE rule 2): per-character key-state pose
sets with the swap/crossfade system, wired to the AI persona keys; the
registry doubles as the human character-selection seam. Characters
whose v3 art exists only as viewer blobs are flagged for a founder
asset export rather than worked around beyond best-available files.

## Relevant rule / requirement IDs

- BB-225 inbox contract; CAST-* boards (cast v3 export `ef0ef7f`);
  DESIGN_ACCEPTANCE rules 1–10; D-42 (PDR-4 resolved: SHOW stands,
  untouched).
- The BB-216 pose system (key states + crossfade) as the baseline.

## Files expected to change

- `apps/web/src/components/game/character-registry.ts` (new) — cast
  characters, pose cells, persona→character mapping.
- `apps/web/src/components/game/negotiation-board.tsx` — resolves the
  opponent character and renders files/sheet/avatar kinds.
- `apps/web/src/components/game/live-match.css` — sheet + avatar
  presentation.
- `apps/web/e2e/cast-poses.spec.ts` — acceptance spec.
- `design-sandbox/screenshots/bb225/` — before/after per persona.
- This manifest.

## Explicitly out of scope

- New character art (export request to the founder, not me); voice;
  title-screen changes; human character selection (data model — the
  registry is the seam); domain/API changes.

## Tests required

- cast-poses spec (persona → kind/source assertions; human keeps
  GoldenOtter); regression friend-match + practice-vs-ai; unit/
  typecheck/lint; before/after screenshots per persona.

## Result

Filled at the end: changed files, behavior, tests, deviations,
asset-export request list.
