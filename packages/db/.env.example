# Local dev defaults — matches docker-compose.yml (not a secret). Copy to .env.
DATABASE_URL=postgresql://bounty:bounty@localhost:5433/bounty_bay
# Shadow DB for non-interactive migration diffs (dev only)
SHADOW_DATABASE_URL=postgresql://bounty:bounty@localhost:5433/bounty_shadow
